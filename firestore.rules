
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For this public app, allow reads and writes to all documents
    // for any signed-in user.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
